 
  ### Client

  1. `$ cd client`
  2. `$ npm install`  
  3. `$ npm start`  

  npm i formic
  npm i yup
  npm i axios

  
### Server
  
  1. `$ cd server`
  2. `$ npm install` 
  3. `$ npm rum devStart`

  npm i bcrypt
  npm i express
  npm i mysql
  npm i nodemon
 


## Used Libraries


### Front-end
+ `formik` 
+ `yup` 
+ `axios` 

### Back-end

+ `bcrypt ` 
+ `express`
+ `mysql`
+ `nodemon`

## Unit testing

npm install @testing-library/react react-test-render jest-dom --save-dev
